class FormPrimer
{
    constructor() {}

    serialize(formId) {
        // var formData = $("#" + formId).serializeArray();
        // window.localStorage.setItem(formId, JSON.stringify(formData));
    }
}

export default FormPrimer;
